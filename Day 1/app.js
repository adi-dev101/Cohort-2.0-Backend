const express = require("express") // importing express module and assigning it to express variable

const app = express() // creating a server instance and assigning it to app variable

app.listen(3000) // server listening on port 3000